#!/usr/bin/env python3

import sys

file_name = sys.argv[1]

with open(file_name, 'r') as f:
	print(f)
#	word_text_list = f.split('.')
#	print(word_text_list)
#	for word in f:
#		word1 = item.split(' ')[0]
#		print(word1)
#		print(word_text)


#Return first word in each sentence
#print first word as a string
